# Exchange Market Web

## Installation

```bash
# yarn install does not work with RBH logger, so npm for install only
$ npm install
$ npx husky install # Git hooks
```

## Running the app

```bash
npm run dev
# or
yarn dev
```
